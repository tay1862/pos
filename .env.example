# ── Database ──
DATABASE_URL="postgresql://pos_user:pos_secret@localhost:5432/pos_db"
POSTGRES_DB=pos_db
POSTGRES_USER=pos_user
POSTGRES_PASSWORD=pos_secret

# ── API ──
API_PORT=3000
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d

# ── Redis ──
REDIS_URL=redis://localhost:6379
